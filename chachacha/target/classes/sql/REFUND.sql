CREATE TABLE REFUND(
R_ID VARCHAR2(10) REFERENCES BUYER(B_ID),
R_CODE VARCHAR2(10) REFERENCES PRODUCT(P_CODE),
R_REASON VARCHAR2(1000) NOT NULL ,
R_COUNT	VARCHAR2(10)NOT NULL
)

SELECT * FROM REFUND